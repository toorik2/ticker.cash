{
  "name": "@ticker/web",
  "version": "0.3.0",
  "private": true,
  "type": "module",
  "description": "Static HTML dashboard for the ticker.cash BCH/USD oracle. On-chain consumption only — no JSON API.",
  "scripts": {
    "build": "rm -rf dist && mkdir -p dist && cp index.html docs.html dist/ && cp -r public/. dist/ 2>/dev/null || true",
    "start": "tsx server/server.ts",
    "dev": "tsx watch server/server.ts"
  },
  "dependencies": {
    "express": "^4.21.2"
  },
  "devDependencies": {
    "@types/express": "^5.0.0",
    "@types/node": "^22",
    "tsx": "^4.20.0",
    "typescript": "^5.6.0"
  }
}
