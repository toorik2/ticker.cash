{
  "name": "@ticker/web",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "description": "Standalone HTML pages + Express JSON API for the ticker.cash BCH/USD oracle.",
  "scripts": {
    "build": "rm -rf dist && mkdir -p dist && cp index.html docs.html dist/ && cp -r public/. dist/ 2>/dev/null || true",
    "start": "tsx server/server.ts",
    "dev": "tsx watch server/server.ts"
  },
  "dependencies": {
    "@bitauth/libauth": "3.1.0-next.8",
    "@electrum-cash/network": "^4.0.0",
    "@electrum-cash/tcp-socket": "^1.0.0",
    "express": "^4.21.2"
  },
  "devDependencies": {
    "@types/express": "^5.0.0",
    "@types/node": "^22",
    "tsx": "^4.20.0",
    "typescript": "^5.6.0",
    "vite": "^5.4.11"
  }
}
